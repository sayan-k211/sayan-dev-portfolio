{
  "name": "sayan-dev-portfolio",
  "version": "1.0.0",
  "description": "Full-stack portfolio with Angular & Express",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && node src/index.js"
  },
  "engines": {
    "node": "18.x"
  }
}